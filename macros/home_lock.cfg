# Example Klipper home_lock macro
[gcode_macro home_lock]
gcode:
    # Home all axes
    G28

    # Move the nozzle up slightly (adjust Z value as needed)
    G1 Z10 F300

    # Lock the printer by disabling all motors
    M84